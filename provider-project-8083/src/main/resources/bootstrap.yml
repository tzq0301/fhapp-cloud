server:
  port: 8083

spring:
  application:
    name: provider-project-8083
  profiles:
    active: test
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:12300
      config:
        server-addr: 127.0.0.1:12300
        file-extension: yaml
        namespace: test
